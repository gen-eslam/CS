

import 'package:flutter/material.dart';

class CreateHardware extends StatelessWidget {


  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Create Hardware'),
        centerTitle: true,
      ),
      body: Column(
        children:
        [

        ],
      ),
    );
  }
}
